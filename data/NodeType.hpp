#pragma once

namespace nvyc::data {

enum class NodeType {

    VARDEF, VARIABLE, REDEF, ASSIGN,                    // Variables
    GLOBALVARDEF,

    ADD, SUB, MUL, DIV, LT, GT, GTE, LTE, EQ,           // Arithmetic
    NEQ, MODULO,

    LOOPDEF, LOOPCOND, FORLOOP, WHILELOOP,              // Loops
    LOOPITERATION,

    INT32, INT64, FP32, FP64, STR, CHAR, BOOL_TR,       // Types
    UNIFIED, SINGULAR, TYPE, VOID, BOOL_FA,
    NUM32, NUM64, NUMBER, ARRAY_TYPE, MAP,
    FUNCTIONCHAIN, UNSIGNED, STRUCT, BOOL, SHORT,
    RAWBIN, RAWHEX, ARRAY_SIZE, ARRAY,
    ARRAY_ACCESS, ARRAY_INDEX,

    FINAL, STATIC, PUBLIC, PRIVATE, IMPLICIT,           // Modifiers
    CONSTANT, NATIVE,

    FUNCTION_T, INT32_T, INT64_T, FP32_T, FP64_T,       // Type_T
    STRING_T, CHAR_T, BOOL_T, STR_T,
    TYPE_T, USERTYPE_T, CAST, VOID_T, STAR,

    FUNCTION_STAR, INT32_STAR, INT64_STAR, FP32_STAR,   // Pointers
    FP64_STAR, UNIFIED_STAR,
    STRING_STAR, CHAR_STAR, BOOL_STAR, STR_STAR,
    TYPE_STAR, USERTYPE_STAR, CAST_STAR, VOID_STAR,
    STRUCT_STAR,

    OPENPARENS, CLOSEPARENS, OPENBRKT, CLOSEBRKT,       // Blocking
    ENDOFLINE, COMMADELIMIT, SQUOTE, DQUOTE,
    COMMENT, MLCOMMENTSTART, MLCOMMENTEND,
    OPENBRACE, CLOSEBRACE, ASSUME, USING,
    ATTRIB, ENDOFSTREAM, CONDITION, FSLASH, BSLASH,

    RETURN, RETFUNC, RETNAT, RETTYPE,                   // Return statements

    IF, ELSE, IFRESULTS, TERNARY,                       // Conditionals
    SWITCH, CASE,

    BITAND, BITOR, BITXOR, BITNEGATE, NOT,              // Logic
    ARITHRIGHTSHIFT, ARITHLEFTSHIFT, LOGICRIGHTSHIFT,
    LOGICAND, LOGICOR, LOGICXOR, LOGICNEGATE, SWITCHSIGN,

    NODE, PRINT, SYSCALL,                               // Misc
    PTRDEREF, FINDADDRESS,
    INC, DEC, PROGRAM, FORWARD,
    INVALID, MEMBER,

    FUNCTION, FUNCTIONAPP, ARGUMENT, FUNCTIONRETURN,    // Functions
    BLOCKSTART, BLOCKEND, FUNCTIONNAME,
    FUNCTIONPARAM, FUNCTIONLINE, FUNCTIONCALL,
    FUNCTIONBODY,

    DIRECTIVE, DIRTYPE, DIRVALUE,                       // Directives
    DIRIMPORT, DIRLIBDEF, DIRPRIVATE, DIRALIAS,
    DIRUSERTYPE
}; // NodeType

} // namespace nvyc::data